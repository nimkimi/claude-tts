---
description: Repeat the last thing Sonari spoke
---

Run the Sonari repeat command with the Bash tool:

```
PYTHONPATH="${CLAUDE_PLUGIN_ROOT}/src" python -m sonari.cli repeat
```

This command is silent on success; do not add commentary unless it errors.
