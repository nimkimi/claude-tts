---
description: Skip the current Sonari utterance and move to the next
---

Run the Sonari skip command with the Bash tool:

```
PYTHONPATH="${CLAUDE_PLUGIN_ROOT}/src" python -m sonari.cli skip
```

This command is silent on success; do not add commentary unless it errors.
