---
description: Run Sonari health checks (TTS, voice, daemon, hooks, hotkeys)
---

Run the Sonari doctor command with the Bash tool:

```
bash "${CLAUDE_PLUGIN_ROOT}/bin/sonari" doctor
```

Print the command's output to the user verbatim so they can see each health check.
Do not add commentary beyond the raw output.
