---
description: Remove Sonari's autostart, hotkey helper, launcher, and app copy
---

Run the Sonari uninstall command with the Bash tool:

```
bash "${CLAUDE_PLUGIN_ROOT}/bin/sonari" uninstall
```

Print the command's output to the user verbatim. Do not add commentary beyond
the raw output. If the command errors, report the error briefly.
