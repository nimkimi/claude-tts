---
description: Stop Sonari speech immediately and clear the queue
---

Run the Sonari stop command with the Bash tool:

```
PYTHONPATH="${CLAUDE_PLUGIN_ROOT}/src" python -m sonari.cli stop
```

This command is silent on success; do not add commentary unless it errors.
