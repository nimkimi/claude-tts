---
description: One-time Sonari setup — autostart, global hotkeys, control CLI
---

Run the Sonari install command with the Bash tool:

```
bash "${CLAUDE_PLUGIN_ROOT}/bin/sonari" install
```

Print the command's output to the user verbatim so they can see each setup step.
Do not add commentary beyond the raw output. If the command errors, report the
error briefly.
